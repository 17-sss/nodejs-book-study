<!-- [6.5.2.2] 반복문 -->


<!-- EJS에서는 자바스크립트 코드를 <% %> 안에 쓰면 됨 -->
<ul>
    <% var fruits = ['사과', '바나나', '배', '오렌지']; %>
    <% for (var i=0; i < fruits.length; i++ ) {      %>
        <li><%= (i+1) + '번째 ' + fruits[i] %> </li>
    <% } %>
</ul>